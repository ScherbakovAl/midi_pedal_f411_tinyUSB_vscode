# 🦀 Краткая сводка миграции на Rust

## 📊 Резюме проекта

### Текущее состояние (C/C++)
- **MCU**: STM32F411CEU6 @ 96 MHz
- **Функции**: USB MIDI + HID Keyboard Composite Device
- **Размер**: ~30 KB Flash
- **Стек**: STM32 HAL + TinyUSB + CMake

### Целевое состояние (Rust)
- **MCU**: Тот же STM32F411CEU6
- **Функции**: Идентичная функциональность
- **Размер**: ~25-35 KB Flash (ожидается)
- **Стек**: stm32f4xx-hal + usb-device + Cargo

---

## ✅ Выбранные технологии

### Основные крейты
```toml
[dependencies]
# HAL Layer
stm32f4xx-hal = { version = "0.21", features = ["stm32f411"] }
stm32f4 = "0.15"  # PAC

# USB Stack  
usb-device = "0.3"
synopsys-usb-otg = { version = "0.4", features = ["fs"] }
usbd-midi = "0.2"
usbd-hid = "0.7"

# Embedded Infrastructure
cortex-m = "0.7"
cortex-m-rt = "0.7"
heapless = "0.8"
panic-halt = "0.2"
```

### Обоснование выбора
- ✅ **stm32f4xx-hal** - зрелый HAL с хорошей документацией
- ✅ **usb-device** - стандарт для USB в Rust embedded
- ✅ **synopsys-usb-otg** - поддержка DWC2 контроллера STM32F4
- ✅ **heapless** - коллекции без heap allocation
- ✅ **Без async** - проще для первой версии, можно мигрировать на Embassy позже

---

## 📁 Структура проекта

```
midi_pedal_f411_rust/
├── Cargo.toml                 # Зависимости и настройки
├── .cargo/config.toml         # Toolchain конфигурация
├── memory.x                   # Карта памяти
├── build.rs                   # Build script
├── src/
│   ├── main.rs               # Entry point + main loop
│   ├── peripherals.rs        # Инициализация периферии
│   ├── power.rs              # Power management
│   ├── usb/
│   │   ├── mod.rs            # USB менеджер
│   │   ├── descriptors.rs    # USB дескрипторы
│   │   ├── midi.rs           # MIDI логика
│   │   └── hid.rs            # HID Keyboard
│   └── pedal/
│       ├── mod.rs            # Pedal менеджер
│       ├── gpio.rs           # GPIO + EXTI
│       └── adc.rs            # ADC обработка
└── .embed.toml               # probe-rs config
```

---

## 🎯 Этапы реализации

### Фаза 1: Базовая настройка (1-2 дня)
- [x] Создать Cargo проект
- [ ] Настроить toolchain и memory.x
- [ ] Базовая инициализация STM32F411
- [ ] Hello World с LED

### Фаза 2: Периферия (2-3 дня)
- [ ] GPIO с EXTI прерываниями (4 педали)
- [ ] ADC для expression pedal
- [ ] Таймеры для debounce
- [ ] LED индикация

### Фаза 3: USB (3-4 дня)
- [ ] USB enumeration как composite device
- [ ] MIDI класс (Note On/Off, CC)
- [ ] HID Keyboard класс
- [ ] Дескрипторы (VID/PID/Strings)

### Фаза 4: Логика приложения (2-3 дня)
- [ ] Обработка педалей через очередь
- [ ] MIDI отправка (2 педали → Notes)
- [ ] HID отправка (2 педали → Keys)
- [ ] ADC → MIDI CC 64
- [ ] Debouncing и таймауты

### Фаза 5: Оптимизация (1 неделя)
- [ ] Power management (Standby mode)
- [ ] Оптимизация размера бинарника
- [ ] Тестирование на железе
- [ ] Документация

**Общее время**: 4-5 недель для полной реализации

---

## 📋 Ключевые различия

### Mapping компонентов

| C/C++ | Rust Equivalent | Комментарий |
|-------|-----------------|-------------|
| `HAL_GPIO_Init()` | `gpioa.pa0.into_pull_up_input()` | Type-state pattern |
| `HAL_ADC_Start_IT()` | `adc.read(&mut pin)` | Без interrupts вначале |
| `tud_midi_stream_write()` | `midi.send_message(Message::NoteOn(...))` | Typed API |
| `tud_hid_keyboard_report()` | `hid.push_input(&KeyboardReport{...})` | Typed struct |
| `std::deque<pedals>` | `heapless::Deque<PedalEvent, 8>` | No heap |
| `EXTI0_IRQHandler()` | `#[interrupt] fn EXTI0()` | Macro-based |

### Преимущества Rust

1. **Безопасность памяти**
   - Нет dangling pointers
   - Borrow checker предотвращает race conditions
   - Compile-time проверки

2. **Type Safety**
   - Невозможно использовать неинициализированный GPIO
   - Статическая типизация USB дескрипторов
   - Enum для состояний

3. **Управление зависимостями**
   - Cargo вместо git submodules
   - Semantic versioning
   - Автоматические обновления

4. **Zero-cost abstractions**
   - Высокоуровневый код → эффективный asm
   - Inline оптимизации
   - Const generics

---

## 🚀 Быстрый старт

### 1. Установка инструментов
```bash
# Rust toolchain для ARM Cortex-M4F
rustup target add thumbv7em-none-eabihf

# probe-rs для прошивки
cargo install probe-rs-tools --locked

# Опционально: cargo-binutils для анализа
cargo install cargo-binutils
rustup component add llvm-tools-preview
```

### 2. Создание проекта
```bash
cargo new --bin midi_pedal_f411_rust
cd midi_pedal_f411_rust

# Скопировать Cargo.toml из плана
# Скопировать .cargo/config.toml
# Создать memory.x
```

### 3. Первая сборка
```bash
# Debug
cargo build

# Release (оптимизированная)
cargo build --release

# Проверить размер
cargo size --release -- -A
```

### 4. Прошивка
```bash
# Автоматическая прошивка через probe-rs
cargo run --release

# Или вручную
probe-rs run --chip STM32F411CEUx \
  target/thumbv7em-none-eabihf/release/midi_pedal_f411_rust
```

---

## ⚠️ Потенциальные проблемы

| Проблема | Решение |
|----------|---------|
| **USB enumeration fails** | Проверить clock: USB требует точно 48 MHz |
| **EXTI не работает** | Включить SYSCFG clock + настроить NVIC |
| **Stack overflow** | Увеличить stack size в memory.x |
| **Большой бинарник** | `opt-level = "z"`, `lto = true`, `codegen-units = 1` |
| **Долгая компиляция** | Использовать `--release` только для финальной сборки |

---

## 📚 Полезные ссылки

### Документация
- 📖 [Embedded Rust Book](https://docs.rust-embedded.org/book/)
- 📖 [stm32f4xx-hal Docs](https://docs.rs/stm32f4xx-hal/)
- 📖 [usb-device Docs](https://docs.rs/usb-device/)

### Примеры кода
- 💡 [stm32f4xx-hal Examples](https://github.com/stm32-rs/stm32f4xx-hal/tree/master/examples)
- 💡 [usb-device Examples](https://github.com/rust-embedded-community/usb-device/tree/master/examples)
- 💡 [Keyberon](https://github.com/TeXitoi/keyberon) - USB клавиатура на Rust

### Инструменты
- 🔧 [probe-rs](https://probe.rs/) - Debugging & Flashing
- 🔧 [cargo-embed](https://probe.rs/docs/tools/cargo-embed/)
- 🔧 [defmt](https://defmt.ferrous-systems.com/) - Logging

---

## 📈 Метрики успеха

### Функциональные требования
- ✅ USB composite device (MIDI + HID)
- ✅ 4 педали с EXTI прерываниями
- ✅ ADC для expression → MIDI CC 64
- ✅ MIDI Note On/Off (педали 1-2)
- ✅ HID Keyboard (педали 3-4)
- ✅ Power management (Standby mode)
- ✅ LED индикация

### Технические метрики
- 📏 **Размер**: ≤ 40 KB Flash
- ⚡ **Латентность**: < 1 ms на событие педали
- 🔌 **USB**: Stable enumeration
- 🛡️ **Надежность**: Zero runtime errors
- 📦 **Зависимости**: Минимальные, только необходимые

---

## 🎬 Следующие шаги

Выберите один из вариантов:

### Вариант 1: Полная реализация (рекомендуется)
Я могу переключиться в **Code mode** и начать пошаговую реализацию:
1. Создать структуру проекта
2. Настроить Cargo.toml и конфиги
3. Реализовать базовую инициализацию
4. Добавить USB функциональность
5. Портировать логику приложения

### Вариант 2: Proof of Concept
Создать минимальный рабочий прототип:
- Базовая инициализация STM32F411
- USB enumeration
- Одна педаль → MIDI Note
- Тестирование концепции

### Вариант 3: Поэтапная миграция
Параллельное существование C и Rust:
- Rust для новых функций
- Постепенная замена C модулей
- Использование FFI при необходимости

### Вариант 4: Дополнительное планирование
- Детализация конкретных модулей
- Создание mock-тестов
- Анализ edge cases

---

## 💭 Вопросы для обсуждения

1. **Приоритет**: Что важнее - скорость миграции или минимальный размер кода?
2. **Async**: Сразу использовать Embassy или начать с синхронного кода?
3. **Тестирование**: Нужны ли unit-тесты для embedded кода?
4. **Отладка**: Какой debugger используется (ST-Link, J-Link)?
5. **CI/CD**: Нужна ли автоматическая сборка и тесты?

---

**Готов начать реализацию! Какой вариант предпочитаете?** 🚀